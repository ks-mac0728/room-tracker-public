# 開発進捗

## 最終更新
2026-07-17

## 運用メモ（重要）
- **`Index.html`をpushしたら、毎回jsDelivrのキャッシュを手動でパージすること**（`curl "https://purge.jsdelivr.net/gh/ks-mac0728/room-tracker-public@main/Index.html"`）。`s-maxage=43200`（12時間）でキャッシュされるため、パージしないと最大12時間ほど古いUIが配信され続ける。実際にこれが原因でユーザーが「UIが変わっていない」と気づいた事例あり（2026-07-17）

## 最後の作業環境
- 端末: 開発Mac
- 日時: 2026-07-17
- RPA代行検索オンボーディング案の再審議・最終判断（却下維持・日替わりおすすめリスト方式を代替案として推奨）を実施
- データ収集をiOSショートカット方式に作り直し、その後さらにブックマークレット方式に切り替え。マスターテンプレート・GitHubにpush済み（詳細は下記）

## 2026-07-17（追記4）記録手段をiOSショートカットからブックマークレットに切り替え

- ダッシュボードに「▶ 記録する」（shortcuts://run-shortcut）ボタンを追加した後、ユーザーから「そもそもショートカットの手動組み立てを無くせないか」と再三の指摘を受けた
- `gen_shortcut.py`のような、タップでインストールできる`.shortcut`ファイルの自動生成を検討したが、実装前に`is.workflow.actions.runjavascriptonwebpage`アクションの公式定義（npm `scpl-macos-updated`パッケージ内のアクション定義データより）を確認したところ、**「Safari Webpage」という入力は共有シート経由のAction Extensionとして実行された場合のみ利用可能、という記述**があり、「Safariで開く→少し待つ→JS実行」を1つのショートカットとして自動生成しても正しく繋がる保証がないと判明（データが古い可能性もあり断定はできないが、リスクを取ってまで自動生成するのは見送った）
- 代わりに、より枯れた標準機能である**ブックマークレット**（`javascript:...`のブックマーク）方式を採用。`navigator.sendBeacon()`でGASへ送信することで、`fetch`で懸念していたCORSレスポンス読み取り問題も回避できる（送信のみで応答を読まないため）
- ダッシュボード（`Index.html`）に、本人専用のWebアプリURLを埋め込んだブックマークレットコードを生成し、「コードをコピー」ボタンで提供する機能を実装。iOSに限らずAndroid・PCのブラウザでも同じ手順で使えるはず（未検証）というメリットもある
- iOSショートカット方式は「上級者向け・任意」としてREADMEに残したが、上記の不確実性がある旨も明記した
- **未完了**：ブックマークレット方式もエンドツーエンドでの実機テストがまだ済んでいない

## 2026-07-17（追記3）データ収集をiOSショートカット方式に実装し直し（タスク#1完了、その後ブックマークレットに置き換え）

投稿・商品選定機能は後回しにし、トラッキング機能に絞って以下を実装、マスターテンプレート・GitHub両方にpush済み。

- `Code.js`: GAS側からの直接取得コード（`fetchAndRecord`・`_extractInitialState`・`dailyFetch`・時間主導トリガー）を全て削除。代わりに`doPost(e)`を復活させ、iOSショートカットからのPOSTを受け取って`_writeRecord`する方式に戻した（`api_secret_key`によるオプションの認証チェックも復活）
- `refreshNow()`を`refreshView()`に改名。GASはROOMへ直接アクセスできないため、「更新」ボタンは「記録」シートの再読み込みのみを行う（ショートカットで新しく送信された分を反映する用途）
- `setup()`はトリガー設定を行わなくなったため、「設定」シートの存在確認とログ出力だけの簡単な関数に縮小
- `ios_shortcut_js.js`を復活（`.claspignore`にも再度追加）。ただし中身は刷新し、DOM文字列を正規表現で探す旧方式ではなく、今回の検証で判明した`window.__INITIAL_STATE__`を直接読む方式にした（より確実）
- `appsscript.json`から`script.scriptapp`スコープを削除（トリガー作成が不要になったため。承認画面がその分シンプルになる）
- README/セットアップ手順を全面的に書き直し：①コピー ②デプロイ（ダッシュボード閲覧用） ③ダッシュボードでROOM URL入力 ④iOSショートカット作成 ⑤「実行前に尋ねる」をオンにしたままの時刻トリガー（通知タップ運用、完全自動化はできない旨を明記）
- マスターテンプレート: https://docs.google.com/spreadsheets/d/1qVV9hLaQgs1oshvd4ZtyuUtvxxGcxp1v-fNJdJVHR2k/copy

**未完了**：この新しい実装をエンドツーエンドで実機テストしていない（次回、新しくコピーを作ってREADME通りに試す必要がある）

## 2026-07-17（追記）CloudCLI経由の別セッションで出た「商品選定のRPA代行お試し検索」案を検討・却下

- ユーザーがCloudCLI（RPA端末上、Tailscale経由でスマホからアクセスする自己ホスト型UI）でGeminiと並行して会話しており、そこで`rakuten-room-poster`の商品選定機能について新しい提案が出ていた
- **提案内容**：APIキー未設定のユーザー向けに、「1ユーザーにつき生涯合計3回まで」、開発者のRPA端末（Mac mini）が代理でRakuten市場を検索する"お試し体験"を提供し、そこからiOSショートカットまたは自分のAPIキー設定へ誘導する3段階ファネル
- 当該Geminiセッション側で`write_file`ツールが使えない不具合が発生しており、本来`PROGRESS.md`に直接追記するはずが書き込めず、一時フォルダ内（`~/.gemini/tmp/room-tracker-public/{sessionId}/plans/rpa-onboarding-proposal.md`）に代替保存されていた。Claude側から直接SSHでその内容を読み取り、正式にここへ記録する
- **評価・却下**：「回数を絞ればリスクが下がる」という提案側の理由づけには同意できないと判断した。理由は、回数を絞っても「1つの主体（開発者のRPA端末）が、多数の異なるユーザーの代わりに自動検索する」という構造自体は変わらず、利用者が増えるほど累計アクセス量が増えて楽天側から見た通信パターンが悪化するため。加えて「楽天ショッピングサービスご利用規約」には「自動化された手段を用いて商品ページ上の情報を取得する行為」を禁止する条項がある（`gen_shortcut.py`削除より前、2026-07-16のROOM規約調査とは別に、楽天市場側の規約として確認済み）。商品選定には公式の楽天ウェブサービスAPIという正規ルートが既にあるため、わざわざこのリスクを取る理由がないと判断し、**この提案は却下**
- 一方、「導入初期に"魔法のような体験"を用意する」という狙い自体は理にかなっているため、APIキー登録のハードルを下げる別の導線（分かりやすいスクリーンショット付きガイド等、rakuten-room-poster側で既に構想済み）で解決する方針

### 2026-07-17（追記2）却下の再審議依頼と最終判断

- 同じ提案について、ユーザーが「試さないでAPI登録させるのは無理」という強い主張とともに、CloudCLI側のGeminiセッション経由でClaudeへの再検討を依頼。Gemini側からは「論点整理シート」として（1）規約リスクをゼロにしつつお試し体験を実現する代替案はあるか、（2）「生涯3回」という上限でもAkamai等の検知対象になり得るか、（3）RPA代行検索の代わりに「開発者が毎日ピックアップした"本日のおすすめ商品30件"を配信する」方式で代替できないか、という3点の質問が提示された
- **なお、この再審議依頼のやり取り自体も、Geminiセッションの`write_file`ツールが（CloudCLI側の権限モード調整・`codebase_investigator`サブエージェント無効化を試みた後も）使えず、`PROGRESS.md`への直接書き込みができなかった。原因はセッションのワークスペースが`room-tracker-public`プロジェクトに固定されており、`dev-environment`側のPROGRESS.mdなど別プロジェクトへの書き込みはワークスペース境界外として許可されない仕様と推定される（バグというより意図されたサンドボックス制限の可能性が高い）。今回はClaudeが開発Mac上から直接この`PROGRESS.md`（room-tracker-public自身のもの）に記録**
- **Claudeの最終判断：却下は維持。ただし(3)の代替案は採用に値する**
  - (2)への回答：「検知されるか」という論点自体がずれている。問題は検知精度や確率ではなく、「1つの発信元が、無関係な多数のユーザーの代わりに自動取得する」という構造そのものが規約違反であること。上限を絞っても構造は変わらないため、この方向での再反論は成立しないと判断
  - (3)への回答：**これは元の提案と構造が根本的に異なり、採用を推奨**。「ユーザーが検索するたびに代行取得」ではなく「開発者が1日1回、全ユーザー共通の固定リストを取得」という設計にすれば、利用者数が増えても楽天への通信量は1日1回のまま変わらない。却下理由だった「利用者が増えるほど累積アクセスが悪化する」という構造的問題がそもそも発生しない。2026-07-16に採用した「個人利用スケールの自動取得は規約上許容範囲」という判断とも整合する
  - **推奨する代替体験**：「生涯3回の代行検索」ではなく、「毎日更新される全ユーザー共通のおすすめ商品リスト（例：30件）を見て、気に入ったものをワンタップで自分のROOMに登録し、その流れで自分のAPIキー設定に誘導する」という体験に置き換える。「押すだけで魔法のような体験」というUX上の狙いを維持しつつ、規約リスクを構造的に解消できる
- **次にやること**：この代替案（日替わりおすすめリスト方式）の具体的な実装設計を、rakuten-room-poster側の既存のオンボーディング導線と合わせて検討する

## 2026-07-17 にやったこと（重大発見：GASサーバー側取得は技術的に不可能と判明、設計を再度見直し中）

### 実装した内容（マスターテンプレート・GitHubにpush済み）
- マスターテンプレートを`clasp create`で新規作成（旧プロジェクトの`.clasp.json`復旧は断念し、クリーンな状態で作り直した）
  - スプレッドシート: https://drive.google.com/open?id=1qVV9hLaQgs1oshvd4ZtyuUtvxxGcxp1v-fNJdJVHR2k
  - コピー用リンク: https://docs.google.com/spreadsheets/d/1qVV9hLaQgs1oshvd4ZtyuUtvxxGcxp1v-fNJdJVHR2k/copy
  - scriptId: `10ObYLQLSVwZWkqHwz5OnHQJjYEwqttSjLKuFweXAkiGr7iuz3lDCxw9x`
- `setup()`をファイル先頭の関数にし、Apps Scriptエディタで関数選択せずそのまま実行できるようにした
- `onOpen()`シンプルトリガーで、コピー直後に「設定」シートを自動生成（room_url等の空欄を用意）
- Apps Script APIによる自己デプロイを試みたが失敗。**コピーごとに新規発行される「デフォルトのGCPプロジェクト」ではApps Script APIが無効化されており、有効化にはCloud Console操作が必要**（非エンジニアには非現実的、Apps Scriptのデプロイ画面よりもむしろ心理的ハードルが高い）と判明したため撤回。データ収集用の時間主導トリガー設定と、ダッシュボード閲覧用の手動デプロイ（最初の1回のみ）を分離する設計に変更
- UIをCDN配信化：`doGet()`がGitHub上の`Index.html`をjsDelivr経由で毎回取得して配信する方式にした。開発者がUIを更新すれば、既にコピー済みの利用者にも（jsDelivrのキャッシュ次第で数時間以内に）反映される。バックエンド（Code.js、ROOM取得・記録ロジック）は各ユーザーのプロジェクトに閉じたまま
- 汎用の`saveSetting(key, value)`（ユーザーローカルな設定保存、開発者には送られない）と`submitInfo(key, value)`（ユーザーが自ら差し出した情報を開発者管理のフォームに転送）を追加。UI側に新項目を足す時、Code.js側を都度更新しなくて済むようにする狙い
- ダッシュボードに初回オンボーディングフォーム（ROOM URL入力＋任意のメール受け取りオプトイン）を実装
- 「意見募集」「お知らせバナー」等の実装過程で、メール収集は**強制ではなく任意のオプトインにすべき**という判断で確定（公開コードである以上、強制収集は信頼を損なうため却下）

### 重大発見：楽天ROOMがAkamaiのボット対策でGASサーバーを遮断している
- ダッシュボードの初回入力でROOM URLを保存しようとするとHTTP 403エラーが発生
- `UrlFetchApp`にブラウザ風のUser-Agentを付与しても解消せず
- 実行ログでレスポンス本文を確認したところ、`errors.edgesuite.net`（Akamaiのボット対策サービス）による「Access Denied」ページであることが判明
- **これはヘッダーの偽装では回避できない、IPアドレス／通信フィンガープリント単位のブロックである**と結論。GAS（Google Cloud基盤）から送信されるリクエストは、個人利用か大量利用かに関わらず、送信元インフラそのものが一律ブロックされる
- 結果、「GASの時間主導トリガーでROOMページを自動取得する」という2026-07-16に決めた設計全体が、技術的に実現不可能と判明。これは規約解釈の問題ではなく、純粋な技術的制約

### 代替案の検討（すべて棄却、または限定的にのみ有効）
- **本物のブラウザ（各ユーザーのSafari）に戻す** → Akamaiを回避できることは確認済み（従来のiOSショートカット方式）。ただし「完全放置の自動化」は不可能（後述）
- **バックグラウンド自動実行の再検証** → 既存のテスト結果（2026-07-16、ロック中はSafari＋JS実行が動作しない）を再確認。トリガーの種類（時刻・位置情報等）を変えても、iOSの「ロック中はWeb描画・JS実行を止める」というOS制約自体は回避不可能と再確認
- **スクリーンショット＋OCR案** → ロック中はそもそも画面が描画されないため無効
- **独自iOSアプリ＋WebView案** → 開発規模が大きく現実的でない上、WebViewのバックグラウンド描画もSafari同様に制限されているため解決にならない
- **プロキシ/別サーバー経由でAkamaiを回避する案** → 技術的には可能だが、「ボット対策を意図的に迂回する」行為であり、今までの「本物のブラウザと見分けがつかないから安全」という前提とは性質が異なる。しかもコードは公開リポジトリなので発見リスクも高い。**却下**
- **ヘッドレスブラウザ／Python+Playwright/Selenium／RPA端末** → 技術的には有効（本物のブラウザ＋非データセンターIPなら人間の閲覧と区別困難）。ただし常時起動端末が必要で、個人利用（開発者自身のアカウント）には引き続き有効だが、配布するテンプレートの一般ユーザーには非現実的
- **開発者のRPA端末で複数ユーザー分を代行巡回** → たとえ「本人の同意（提出）」があっても、「1つの発信元が多数の異なるアカウントに継続的アクセスする」という構造自体は変わらず、個人利用とは異なるリスクカテゴリになるため却下
- **ユーザー同士の相互カバー型（Peer方式）** → 各参加者が自分の端末で、自分の分＋割り当てられた他人の分を本物のブラウザで開く。取得自体は分散されたまま。ただし「中央の割り当て役（誰が誰をチェックするか管理する軽量なGAS Webアプリ。楽天には直接アクセスしないためAkamaiとは無関係）」が新たに必要になり、参加者不足の立ち上げ初期は開発者が人力（非自動化、手動操作）で穴埋めする前提。特定端末への偏りを避け、多数に薄く分散させる設計が必要。**現時点では構想止まり、実装は未着手**
- **公開情報化してWeb訪問者に見てもらう／バナー誘導** → 実際にデータを抜き出すには本人の端末で抽出用スクリプト（ショートカット等）が動いている必要があり、単なる閲覧・クリックだけでは何のデータも得られないため無効

### 収益化・エンゲージメント施策の新しいアイデア
- 姉妹プロジェクト`rakuten-room-poster`のGemini API（AI投稿文生成）を**中央でクレジット管理**し、ROOMチェックイン（トラッカーの利用）でクレジット付与する施策を検討。Gemini APIは楽天ウェブサービスAPIと異なりアカウント間共有を禁止する規約がないため、中央管理が可能。「本人が残クレジットを把握した上で使う」設計にすれば、poster側の競合調査で批判した「黙ってフォールバックする」アンチパターンとは別物になる
- 一方、`rakuten-room-poster`の商品選定機能（楽天市場API）は、楽天ウェブサービス利用規約（第5条2項・第10条7項）により**アカウント間共有が明確に禁止されている**ため、中央管理不可。各ユーザーが自分で楽天デベロッパー登録をする手間は避けられない
- 買い切りライセンスキーによるプレミアム機能開放構想も再確認（CDN配信のUIと組み合わせ、開発者管理の軽量な検証エンドポイントでキーの有効性を確認する設計）

### 今日の結論
- 今日組み立てたCDN配信UI・汎用設定/情報保存関数・onOpen自動生成などのインフラ部分は有効なまま活かせる
- **データ収集の実装（`fetchAndRecord`をGASの時間主導トリガーから呼ぶ現在のコード）は、Akamaiにブロックされるため実際には機能しない。本物のブラウザ（iOSショートカット等）を使う方式に戻す実装のやり直しが必要**（未着手）
- スコープが「無料版トラッカー」から「投稿ツールとのバンドル」「相互カバー型クローリング」「AIクレジット」「ライセンス課金」まで一気に広がったため、いったん優先順位を「今すぐ必要なもの」と「将来構想」に仕分けし、区切りをつけることにした
- 追加で「ブログ等で紹介し、第三者にプロフィールをクリックしてもらう」「クリック・ブラウザ読み込みのイベントを中継サイトで捕捉する」という案も検討したが、いずれも同じ理由（データ抽出の仕組みは訪問者の端末に事前に入っている必要があり、単なるクリック・閲覧では中身のデータは取得できない）で不成立と確認
- **最終結論**：「システム（ショートカット等）を導入済みの本人が、自分の意思でブラウザを開く」以外に、ROOMデータを取得する方法は無いことを確認。次回はこれを前提に、iOSショートカット方式への実装のやり直しに着手する

## 2026-07-16 にやったこと（配布方式・データ収集方式の再設計）
- ローカルフォルダを移動した影響を調査。`.clasp.json`（GASプロジェクトの紐付け、.gitignore対象）が消失していることを発見。`clasp list`（kousuke@multiuse.xyz）にも該当スクリプトが出てこず、未復旧（scriptIdをApps Scriptエディタから手動で確認してもらう必要あり）
- 配布方式を再検討し、以下を却下：
  - Webサービス化（多ユーザー分のホスティング・認証を自前で持つコスト）
  - Googleワークスペースアドオン化（センシティブ/制限付きスコープのOAuth審査・CASA監査が数週間〜年次で必要、無料テンプレートには重すぎる）
  - Googleフォームでの受け口簡略化（コピー後にentry.IDを手作業で調べて転記する必要があり、番号ミスが起きても気づきにくい）
- 「個別GASテンプレート配布（スプレッドシートをコピーして使う）」を維持することで確定
- スマホ専用ユーザー向け導入フローを検討したが、Apps Scriptの「デプロイ」画面自体がPC向けUIで、スマホでは「PC版サイトをリクエスト」が必須になるなど、実態は「PCでの初期セットアップを前提」と判明。README表現を「初期セットアップはPC推奨、日常の記録・閲覧はスマホでOK」に見直す方針
- iOSショートカットの完全放置バックグラウンド自動実行を実機テスト → ロック中は発火・完了せず失敗を確認済み（「実行前に尋ねる」オフでも、Safari＋JS実行アクションはロック中に動かない）
- 楽天ROOM側の技術検証（curlで生HTML取得、ログアウト状態）：
  - 公開プロフィールページに`__INITIAL_STATE__`がSSRで埋め込まれており、`followers`/`following_users`/`collections`/`likes`/`rank`等がログイン・JS実行なしで取得できることを確認（ヘッドレスブラウザは不要。以前「必要」と伝えたのは誤りだったため訂正済み）
  - `X-Frame-Options: SAMEORIGIN`・CSP `frame-ancestors`・`Access-Control-Allow-Origin`なしを確認。ダッシュボード内へのiframe埋め込み／fetchでの読み取りは不可能と判明
- ROOM利用規約（https://room.rakuten.co.jp/info/agreement.html）を確認。第11条1項4号は「スクリプト等による機械的な投稿・コメント」を禁止しているが、閲覧の自動化を明示的に禁止する条文は見当たらなかった（断定はできないため、実運用前に本人が原文を確認する前提）
- 姉妹プロジェクト`rakuten-room-poster`（半自動投稿ツール、投稿自体は手動クリックのみ）の設計を確認。楽天Web APIとGemini APIを利用者自身のキーで使う設計で、競合調査の結果も含め規約遵守の観点で室-tracker-publicと一貫していることを確認済み
- **最終決定**：データ収集はiOSショートカットをやめ、GASの時間主導トリガーで設定シートに登録したROOM URLを定期的に自動取得する方式に変更。「個人利用スケール・読み取り専用・無料配布」であれば規約上のリスクは薄いグレーとして許容し、商用化・大規模化する段階で再検討する
- 無料版の追加機能候補として、(a) ユーザーが明示的に指定した少数の競合/目標アカウントを同じ仕組みで追跡、(b) 取得済みJSONに含まれる`recollected`（拡散力の指標）等の活用、(c) 週次増減・4週平均比などの派生指標カード（データ計算のみ、収集追加なし）を検討
- 開発者自身が多数アカウントを無断でクローリングして公開する案は、規約リスクが最大・個人情報の再配布問題もあるため却下
- フェーズ2構想として、「利用者の同意（オプトイン）を得た上で、各自が自分のツールで収集したデータを開発者側のプラットフォームに提供し、ベンチマーク/リーダーボードとして公開する」機能を検討。中央側は楽天に直接アクセスしない設計のためスクレイピング問題を回避できるが、共有バックエンドの運用コストは別途発生するため、まず無料の個別配布版を出してから着手する方針
- 販売方針は「まず無料版を出し、実際の需要を見てから買い切りキット等の販売を検討する」に転換。プロ向け機能拡充・投稿ツールとのバンドル販売は保留

## 前回（2026-07-15まで）やったこと
- Code.js / Index.html / ios_shortcut_js.js / appsscript.json / README.md を実装
- clasp で GAS プロジェクト作成・コードプッシュ・WebAppデプロイ
- ios_shortcut_js.js が GAS サーバー側に混入してエラー → .claspignore で修正・再デプロイ
- ダッシュボード表示を確認済み
- ios_shortcut_js.js に `action: "receiveStats"` を含めてショートカットを2ステップに簡略化
- shortcut_template.shortcut（タップでインストール用）を生成・GitHubに公開
- GitHubリポジトリをpublicに変更済み
- jsDelivr経由でのファイル配信は成功（200）だが、iOS側で「未署名のショートカット」エラーが出てインストール不可

## WebアプリURL（デプロイ済み）
```
https://script.google.com/macros/s/AKfycbyzLvVzPzT16OzGwAwYbFstGJyUqeTve1R3r-wIqbyvfdWZpPX9BeOXqdjDAS8muIoocg/exec
```

## スプレッドシートURL
```
https://drive.google.com/open?id=1F6hXasIeIco3l6JSPOEKU0ySSBViiO4mZ9-ZT40q-9g
```

## ショートカット配布URL（現状）
```
https://cdn.jsdelivr.net/gh/ks-mac0728/room-tracker-public@main/shortcut_template.shortcut
```
※ iOS側で「信頼されていないショートカット」エラーになる。設定→ショートカット→「信頼されていないショートカットを許可」をオンにすれば回避可能。

## 次にやること（優先順）
1. **【最優先】データ収集を「本物のブラウザ（iOSショートカット）」方式に作り直す** — 現在のCode.jsの`fetchAndRecord`（GAS時間主導トリガー経由）はAkamaiにブロックされ実際には機能しないため、Safari＋JS実行＋GASへPOSTという旧方式（2026-07-15時点の設計に近いもの）に戻す必要がある。ただし完全放置の自動実行は不可能と実証済みのため、「通知タップで手動実行」を前提とする
2. 派生指標カード（週次増減・4週平均比・目標達成予測・急増急減ハイライト）の実装
3. ダッシュボードの画像シェア機能・お知らせバナー・意見募集リンクの実装（task #6〜#8として登録済み、CDN配信UIの仕組みは既にできているのでIndex.htmlの更新だけで反映可能）
4. （保留・将来構想）ユーザー同士の相互カバー型クローリング、AIクレジット中央管理によるチェックイン施策、投稿ツールとのバンドル、ライセンスキーによる課金——これらは「今すぐ必要なもの」の実装が落ち着いてから着手する

## 未完了の課題
- **データ収集の実装がAkamaiブロックにより機能しない状態**。iOSショートカット方式への作り直しが最優先の未完了タスク
- Apps Script APIによる自己デプロイは撤回済み。ダッシュボード閲覧用のWebアプリデプロイは、初回のみ手動（README記載の手順）で行う前提
- Chromeでは使えない（Safari専用、本物のブラウザ経由の取得に戻すため）

## ユーザーの意図・構想
- 楽天ROOMの成果をトラッキングするダッシュボードを他ユーザーに配布したい
- スマホ（iOS）ショートカット＋GAS WebAppで完結させる（RPA端末不要）
- データ収集はiOSショートカットが担当（ユーザー自身のブラウザで実行 → スクレイピングに当たらない）
- 毎日の通知を1タップするだけ、またはダッシュボードの「更新」ボタンで手動更新
- Threads投稿機能はスコープ外（ダッシュボード機能のみ）
- 既存の rakuten-room-tracker とは完全に別プロジェクトとして管理
- データスキーマは既存の rakuten-room-tracker と同一（followers/following/posts/likes/rank）
- 他ユーザーがコピーして使えるテンプレートとして配布
- （2026-07-16）このトラッカーの存在価値は「時系列推移が見えること」の一点。楽天ROOM自体が「今の値」は表示しているという前提を踏まえた上での差別化ポイントとして認識している
- （2026-07-16）配布戦略を「無料版を先に出して実際の需要を見てから販売を検討する」方針に転換。当初検討していた「プロ向け機能拡充」「投稿ツールとのバンドル販売」「Web対応/Googleアドオン化」は全て保留とし、まず無料の個別配布版の完成を優先する
- （2026-07-16）個人利用スケール（自分のデータを読むだけ、または自分が明示的に指定した少数の相手を読むだけ）であればGASによる自動取得は規約上許容範囲、という リスク判断を採用。無断・大規模なクローリング（不特定多数を対象にする、または開発者が一括収集して公開する）は明確に却下
- （2026-07-16）将来的に「利用者の同意を得た上でのデータ共有・ベンチマーク公開」機能をフェーズ2として構想。中央側が楽天に直接アクセスしない設計にすることでスクレイピングリスクを回避する
- （2026-07-16）姉妹プロジェクト`rakuten-room-poster`（投稿補助ツール、投稿は手動）との統合は将来候補。ただし公開テンプレートに個人専用の連携コードを埋め込むと他ユーザー環境で壊れるため、統合するなら両方をセットで配布する形にする必要がある
- （2026-07-17）「1日1回のタップが必要」という制約は、ユーザーにとって受け入れがたい負担（"サービスとして致命的"）だと考えている。Duolingo等の習慣化アプリの例を挙げて説得を試みたが、本人の判断は変わらず、この負担を解消する仕組み（相互カバー型クローリング等）を追求する方針
- （2026-07-17）個人としての運用（自分のROOMアカウントの追跡）は、既存のRPA端末（rakuten-room-trackerと同じ仕組み）を使うことで解決する意向。公開テンプレート側は別の仕組みが必要という2本立ての理解で合意済み
- （2026-07-17）ユーザーは、規約リスクの判断において「見る人が誰か」より「取得を実行する主体が集約されているか」が本質的な基準だと理解し、これに同意している
- （2026-07-17）AIクレジットの中央管理によるエンゲージメント施策（チェックインで付与）は、rakuten-room-poster側で批判した「黙ったフォールバック」とは違う、透明性のある共有プールとして設計すれば問題ないという整理で合意
