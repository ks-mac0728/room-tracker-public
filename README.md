# 楽天ROOMトラッカー（公開版）

楽天ROOMのフォロワー数・投稿数・いいね数を毎日自動で記録し、GASのWebアプリでグラフ表示するダッシュボードです。

## 構成

```
Code.js           — GAS バックエンド（自動取得・スプレッドシート書込・ダッシュボード配信・自己デプロイ）
Index.html        — ダッシュボード UI
appsscript.json   — GAS マニフェスト
```

## 特徴

- データ取得は完全自動（毎日決まった時刻にGASが自分のROOMプロフィールを取得・記録）
- スマホアプリのインストールや、iOSショートカットの作成は不要
- 初期セットアップだけはPCでの作業を推奨します（Apps Scriptのデプロイ画面がスマホでは操作しにくいため）。セットアップ後の日常利用（ダッシュボードを見る）はスマホで問題ありません

## セットアップ手順（PC推奨）

スマホのみで作業する場合、画面が正しく表示されない・拡張機能メニューが出ない時は、ブラウザのメニュー（Safariなら「aA」、Chromeなら「⋮」）から「デスクトップ用Webサイトを表示」をタップしてください。セットアップの間だけ使う一時的な切り替えで十分です。

### 1. テンプレートをコピーする

以下のテンプレートのリンク末尾を `/copy` にしたURLを開くと、自動的に「コピーを作成」の確認画面が表示されます。「コピーを作成」を押すと、あなたのGoogleドライブに複製されます（コード一式も一緒にコピーされます）。コピーを開いた時点で「設定」シートが自動的に用意されます。

### 2. セットアップを実行する

1. スプレッドシートのメニューから「拡張機能」→「Apps Script」を開く
2. 何も選び直さず、そのまま▶（実行）ボタンを押す（`setup`が先頭の関数として選ばれています）
3. 初回のみ権限確認画面が出るので、「権限を確認」→ Googleアカウントを選択 →「詳細」→「(プロジェクト名)に移動（安全ではないページ）」→「許可」と進む
4. 実行が完了すると、自動で以下が設定されます
   - 毎日指定した時刻にROOMプロフィールを取得・記録する時間主導トリガー（デフォルトは毎日7時。変更したい場合は「設定」シートの`fetch_hour`を編集してから再度`setup`を実行）
   - このスクリプト自身をWebアプリとしてデプロイ
5. 実行ログ（画面下部）にダッシュボードのURLが表示されるほか、あなたのGoogleアカウントのメールアドレス宛にもURLが届きます

### 3. ROOM URLを入力してはじめる

手順2で表示されたダッシュボードURLをブラウザで開きます。初回は「あなたのROOM URLを入力してください」という画面が出るので、あなたの楽天ROOMプロフィールURL（例: `https://room.rakuten.co.jp/room_xxx`）を入力して「保存してはじめる」を押します。これでダッシュボードが表示され、以降は毎日自動で記録されます。スマホのホーム画面に追加しておくと、日々の確認が楽になります。

## データスキーマ

スプレッドシートの「記録」シートに以下の形式で記録されます（1日1回、同日は上書き）。

| 日時 | URL | フォロワー数 | フォロー数 | 投稿数 | いいね数 | ランク |
|------|-----|------------|----------|-------|--------|------|

## トラブルシューティング

**ダッシュボードが表示されない・データが記録されない**
- 「設定」シートの `room_url` が正しく入力されているか確認してください
- `setup` を再実行すると、トリガーとデプロイが作り直されます

**「権限を確認」の画面で不安に感じる**
- このスクリプトは自分のコピーしたスプレッドシートに対してのみ動作し、外部に送信されるのはあなたが入力したROOM URLへのアクセスのみです。「Google で確認されていないアプリ」という警告は、個人が作成・複製したスクリプトでは通常表示されるもので、異常ではありません

**急にダッシュボードの表示がおかしくなった**
- `setup` をもう一度実行すると、最新のコードで再デプロイされます

## 意見・要望

このツールについての感想・要望は、あなたが用意したフィードバック用フォームへのリンクをここに掲載してください。
